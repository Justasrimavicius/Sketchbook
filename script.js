const gridContainer=document.getElementById('grid');
//grid width,height is 400px

// dimensions of the grid are inputed by a slider
function DimensionsSlider(){
    
}


// the dimensions are then returned to CreateGrid function
function CreateGrid(dimensions){

}